"use client";

import Banner from "@/components/banner";

export default function Home() {
  return (
    <>
      <Banner />
      {/* <div className="flex flex-col px-4 py-5 sm:px-6">
        <div className="flex">
          <button onClick={() => dispatch(increment())} className="inline-flex items-center rounded-md bg-indigo-600 mx-3 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">
            Increment
          </button>
          <button onClick={() => dispatch(decrement())} className="tems-center rounded-md bg-indigo-600 mx-3 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">
            Decrement
          </button>
          <button onClick={() => dispatch(incrementByAmount(2))} className="tems-center rounded-md bg-indigo-600 mx-3 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">
            Increment by Amount
          </button>
        </div>
        <span className="flex flex-auto items-center rounded-md bg-white mt-5 mx-3 px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50">
          {count}
        </span>
      </div> */}
    </>
  )
}
